[project]
name = "insurance-dw-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "apache-airflow>=3.1.7",
    "apache-airflow-providers-postgres>=6.4.0",
]
